#
# Sample configuration for running the Streamlit demo locally.
# Copy this file to `.env` and replace placeholders with your credentials.
#

# Required API key used by the OpenAI-compatible client.
OPENAI_API_KEY=your-real-key

# Base URL for OpenAI-compatible services (use the vendor gateway you configured).
OPENAI_API_BASE=https://bedrock.us-east-1.amazonaws.com/openai

# Embedding model identifier for the semantic search page.
OPENAI_EMBEDDING_MODEL=amazon.titan-embed-text

# Translation model identifier for both Translation and Transformer demos.
OPENAI_TRANSLATION_MODEL=amazon.titan-translate

# Optional generation model for the transformer sampling experience.
OPENAI_GENERATION_MODEL=gpt-4o-mini
